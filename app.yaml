runtime: custom
env: flex
manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
  disk_size_gb: 10
network:
  name: ghost-vpc
  subnetwork_name: us-central1
  instance_ip_mode: internal
  forwarded_ports: